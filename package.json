{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "Simple chat",
  "main": "src/index.js",
  "private": true,
  "type": "module",
  "workspaces": ["frontend"],
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "start-server -s ./frontend/build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd frontend && npm ci"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/S1iBa/frontend-project-12.git"
  },
  "author": "Daria",
  "license": "ISC",
  "dependencies": {
    "@hexlet/chat-server": "^2.0.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.1"
  }
}
